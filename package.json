{
  "name": "battery_power_timer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "sass src/index.scss:style.css --style=compressed; # & vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.2.0"
  },
  "dependencies": {
    "sass": "^1.62.1"
  }
}